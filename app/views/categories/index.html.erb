<% @categories.each do |calamity| %>
  <%= calamity.name %>
<% end %>

<%= link_to "Add Category", new_category_path, class: "btn btn-primary" %>
<%= link_to "Home", calamities_path, class: "btn btn-secondary" %>
